import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hyvaauction.app',
  appName: 'hyva-premier-league-2026',
  webDir: 'dist'
};

export default config;
